// AUTO-GENERATED by scripts/generate.mjs from the TestForge OpenAPI v2 document.
// Do not edit by hand — re-run the generator instead.

// Every method returns the parsed JSON body and throws TestForgeApiError on
// a non-2xx response. `request` is injected by src/index.js so auth, retries
// and rate-limit bookkeeping live in one place.

export function buildMethods(request) {
  return {
    /** List milestones */
    listMilestones(slug, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/milestones`, undefined, options);
    },
    /** Create a milestone */
    createMilestone(slug, body, options = {}) {
      return request("POST", `/projects/${encodeURIComponent(slug)}/milestones`, body, options);
    },
    /** Get a milestone */
    getMilestone(slug, id, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/milestones/${encodeURIComponent(id)}`, undefined, options);
    },
    /** Update a milestone */
    updateMilestone(slug, id, body, options = {}) {
      return request("PATCH", `/projects/${encodeURIComponent(slug)}/milestones/${encodeURIComponent(id)}`, body, options);
    },
    /** Delete a milestone */
    deleteMilestone(slug, id, options = {}) {
      return request("DELETE", `/projects/${encodeURIComponent(slug)}/milestones/${encodeURIComponent(id)}`, undefined, options);
    },
    /** List members */
    listMembers(slug, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/members`, undefined, options);
    },
    /** Create a member */
    createMember(slug, body, options = {}) {
      return request("POST", `/projects/${encodeURIComponent(slug)}/members`, body, options);
    },
    /** Get a member */
    getMember(slug, id, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/members/${encodeURIComponent(id)}`, undefined, options);
    },
    /** Update a member */
    updateMember(slug, id, body, options = {}) {
      return request("PATCH", `/projects/${encodeURIComponent(slug)}/members/${encodeURIComponent(id)}`, body, options);
    },
    /** Delete a member */
    deleteMember(slug, id, options = {}) {
      return request("DELETE", `/projects/${encodeURIComponent(slug)}/members/${encodeURIComponent(id)}`, undefined, options);
    },
    /** List webhooks */
    listWebhooks(slug, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/webhooks`, undefined, options);
    },
    /** Create a webhook */
    createWebhook(slug, body, options = {}) {
      return request("POST", `/projects/${encodeURIComponent(slug)}/webhooks`, body, options);
    },
    /** Get a webhook */
    getWebhook(slug, id, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/webhooks/${encodeURIComponent(id)}`, undefined, options);
    },
    /** Update a webhook */
    updateWebhook(slug, id, body, options = {}) {
      return request("PATCH", `/projects/${encodeURIComponent(slug)}/webhooks/${encodeURIComponent(id)}`, body, options);
    },
    /** Delete a webhook */
    deleteWebhook(slug, id, options = {}) {
      return request("DELETE", `/projects/${encodeURIComponent(slug)}/webhooks/${encodeURIComponent(id)}`, undefined, options);
    },
    /** List fields */
    listFields(slug, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/fields`, undefined, options);
    },
    /** Create a field */
    createField(slug, body, options = {}) {
      return request("POST", `/projects/${encodeURIComponent(slug)}/fields`, body, options);
    },
    /** Get a field */
    getField(slug, id, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/fields/${encodeURIComponent(id)}`, undefined, options);
    },
    /** Update a field */
    updateField(slug, id, body, options = {}) {
      return request("PATCH", `/projects/${encodeURIComponent(slug)}/fields/${encodeURIComponent(id)}`, body, options);
    },
    /** Delete a field */
    deleteField(slug, id, options = {}) {
      return request("DELETE", `/projects/${encodeURIComponent(slug)}/fields/${encodeURIComponent(id)}`, undefined, options);
    },
    /** List environments */
    listEnvironments(slug, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/environments`, undefined, options);
    },
    /** Create a environment */
    createEnvironment(slug, body, options = {}) {
      return request("POST", `/projects/${encodeURIComponent(slug)}/environments`, body, options);
    },
    /** Get a environment */
    getEnvironment(slug, id, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/environments/${encodeURIComponent(id)}`, undefined, options);
    },
    /** Update a environment */
    updateEnvironment(slug, id, body, options = {}) {
      return request("PATCH", `/projects/${encodeURIComponent(slug)}/environments/${encodeURIComponent(id)}`, body, options);
    },
    /** Delete a environment */
    deleteEnvironment(slug, id, options = {}) {
      return request("DELETE", `/projects/${encodeURIComponent(slug)}/environments/${encodeURIComponent(id)}`, undefined, options);
    },
    /** List plans */
    listPlans(slug, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/plans`, undefined, options);
    },
    /** Create a plan */
    createPlan(slug, body, options = {}) {
      return request("POST", `/projects/${encodeURIComponent(slug)}/plans`, body, options);
    },
    /** Get a plan */
    getPlan(slug, planId, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/plans/${encodeURIComponent(planId)}`, undefined, options);
    },
    /** Update a plan */
    updatePlan(slug, planId, body, options = {}) {
      return request("PATCH", `/projects/${encodeURIComponent(slug)}/plans/${encodeURIComponent(planId)}`, body, options);
    },
    /** Delete a plan */
    deletePlan(slug, planId, options = {}) {
      return request("DELETE", `/projects/${encodeURIComponent(slug)}/plans/${encodeURIComponent(planId)}`, undefined, options);
    },
    /** List attachments */
    listAttachments(slug, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/attachments`, undefined, options);
    },
    /** Upload an attachment */
    uploadAttachment(slug, body, options = {}) {
      return request("POST", `/projects/${encodeURIComponent(slug)}/attachments`, body, options);
    },
    /** Get attachment metadata */
    getAttachment(slug, id, options = {}) {
      return request("GET", `/projects/${encodeURIComponent(slug)}/attachments/${encodeURIComponent(id)}`, undefined, options);
    },
    /** Delete an attachment */
    deleteAttachment(slug, id, options = {}) {
      return request("DELETE", `/projects/${encodeURIComponent(slug)}/attachments/${encodeURIComponent(id)}`, undefined, options);
    },
  };
}
